interface:
  display_name: "Forum Features"
  short_description: "Boards, threads, posts"
  default_prompt: "Use $forum-features to update forum handlers, routes, or templates."
policy:
  allow_implicit_invocation: true
